<일급 객체란?>
    1. 무명의 리터럴 생성 가능(이름 없이 함수 생성 가능) -> 런타임 생성
        const increase = function(n) {return n++};
    2. 변수나 자료구조에 저장 가능
        const arr = {increase, decrease};
    3. 함수의 매개변수에 전달 가능
    4. 함수 반환값으로 사용

<이점>
    1. 객체와 동일하게 사용 가능
    2. 함수 매개변수, 리턴값으로 사용 가능 -> 함수형 프로그래밍
