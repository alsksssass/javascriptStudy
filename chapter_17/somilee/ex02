구분	                                  <생성자 함수>	 <일반 함수>
호출 방식	                      new 키워드를 사용해 호출	new 없이 호출
this 바인딩	                        새로운 객체를 가리킴	전역 객체(엄격 모드에서는 undefined)를 가리킴
주요 목적	                            새로운 객체 생성	특정 작업이나 로직을 수행
반환 값	        명시적인 return이 없으면, 생성된 객체를 반환	명시적으로 반환된 값이 있으면 그 값을 반환, 없으면 undefined 반환
프로토타입	    생성된 객체는 해당 함수의 prototype을 상속받음	프로토타입과 관련이 없음